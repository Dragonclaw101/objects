console.log('Hello World!');

//This is an object literal - I define every part of the object.
let car = {
    make: 'Ford',
    model: 'F1-50',
    passengers: 5,

}

//print make & model of the car

console.log('My car is a ' + car.make + ' ' + car.model);
console.log('The ' + car.make + ' ' + car.model + ' carries ' + car.passengers + ' passengers');